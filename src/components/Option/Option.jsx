import React from 'react'

const Option = (props) => {
    const {type, active, style, activeStyle, restProps} = props;
    return (
        <div>
            
        </div>
    )
}

export default Option;